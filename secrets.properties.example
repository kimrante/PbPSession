# 저장소에 올리지 않는 값 (.gitignore). 이 파일이 없어도 빌드는 된다 —
# 구글 로그인만 꺼진 채로 동작한다.
#
# 아래 값은 GCP 콘솔 → API 및 서비스 → 사용자 인증 정보에서
# '데스크톱 앱' 유형 OAuth 클라이언트의 클라이언트 보안 비밀번호다.
# (설치형 앱의 시크릿은 원래 완전한 비밀이 될 수 없다 — 바이너리에 들어간다.
#  그래도 저장소에는 두지 않는다.)
desktopGoogleClientSecret=여기에 붙여넣기
